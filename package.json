{
  "name": "fnv",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "description": "Framework-Agnostic Form Validation Library Using the Standard Schema Spec",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^5.1.0",
    "eslint": "^9.32.0",
    "eslint-plugin-format": "^1.0.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
